Web preview for `ScreenHome` using Vite

Quick start:

1. Open a terminal in this folder: `frontend/web-preview`
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`

The preview imports the project's `menuItems.json` and the shared hook `frontend/hooks/useMenuLogic.js`.
